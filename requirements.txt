# Faixas conservadoras: limite inferior testado e teto no próximo major
# para evitar quebras por atualização automática no CI.
# Após uma execução bem-sucedida do pipeline, recomenda-se fixar as versões
# exatas (==) a partir de `pip freeze` para reprodutibilidade total.
google-api-python-client>=2.100,<3
google-auth>=2.23,<3
google-auth-oauthlib>=1.0,<2
google-auth-httplib2>=0.2,<1
gspread>=5.12,<7
pandas>=2.0,<3
numpy>=1.24,<3
